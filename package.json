{
  "name": "nightclub-api",
  "version": "3.0.0",
  "description": "Mock API for the Night Club project",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "reset-db": "node scripts/copy-file.js db_backup.json db.json",
    "seed": "npm run reset-db",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
